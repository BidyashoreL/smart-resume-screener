# Smart Resume Screener

A recruiter screening system that extracts structured candidate data from resumes,
compares it against a job description using hybrid (deterministic + LLM) matching,
and returns a ranked, evidence-backed shortlist.

> Full documentation (architecture, prompts, API reference, setup) is being written
> as the project is built out. See `docs/` for architecture notes in the meantime.
